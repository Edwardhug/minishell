env -i faut quand meme creer pwd, shlvl et underscore
old pwd cree dans export mais pas initialis;e

mkdir 123 cd 123 mkdir 123 cd 123 rm -rf ../../123. cd -> segfault

pwd pas modifié dans l'environnement, que le old pwd

double double guillemets dans parsing export.
leak quand:

-export avec double guillemets
-export puis exit



_ fonctionne MAIS
-si on fait echo $_ ça donne forcément echo alors que:
-echo $_ ne doit pas changer le _
-si on fait echo $_ après avoir fait un echo, _ devient l'argument de echo. Si il y
a plusieurs, ça devient le dernier argument.
-dans le cas des pipes, ça prend celui du premier pipe.
- _ devrait être la commande avec le path dans l'env, mais pas dans echo